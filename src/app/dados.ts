export class dados {
      apelido: String;
      local: String;
      marca: String;
      modelo: String;
      status: Boolean;
      id: Number

constructor(id: Number, apelido: String, local: String, marca: String, modelo: String, status: Boolean,){

    this.id = id;
    this.apelido = apelido;
    this.local = local;
    this.marca = marca;
    this.modelo = modelo;
    this.marca = marca;
    this.status = status;
}
}